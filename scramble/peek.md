# what was i doing last time.
DOING working to make UI work with single level's data GameUILevelState, currently able to send updates back, next need to add validation on submit, validation on each text field (like allow entering only valid letters)
DONE  after fixing data model, now time to fix GameViewModel to start using this new data model and first init the data model statically from code itself.